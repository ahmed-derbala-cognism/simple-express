{
  "name": "simple-express",
  "version": "1.0.0",
  "description": "",
  "main": "simple-express.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "format": "prettier --write \"**/*.js\"",
    "push": "npm run format && git add . && git commit -m",
    "postpush": "git push && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
